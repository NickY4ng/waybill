# 大卡鹰眼 - 改动文档

**日期**: 2026-04-01 ~ 2026-04-02

***

## 一、系统管理功能

新增系统管理后台模块，供内部运营同学进行系统运维管理。

### 1. 账号管理

管理员可在后台进行账号全生命周期管理：

**账号列表字段**

- 账号ID（系统自动生成）
- 关联CID（Apass ID，非必填）
- 客户名称：必填
- 手机号（作为登录账号）：必填
- 邮箱：必填
- 状态（正常/过期/禁用）
- 有效期开始时间：必填
- 有效期结束时间：必填
- 创建人

**账号新增**

- 填写客户名称、手机号、邮箱
- 手机号作为登录账号，需格式校验
- 设置账号有效期（开始时间和结束时间）
- 配置数据权限SQL（可选）
- 系统自动生成初始密码

**账号编辑**

- 修改关联CID
- 客户名称
- 手机号
- 邮箱
- 修改有效期（开始时间和结束时间）
- 修改账号状态（正常/禁用/过期）
- 修改数据权限SQL

**账号删除**

- ~~支持单个删除~~（已移除：不允许删除账号）
- ~~删除前二次确认~~

**账号状态管理**

- 正常：可正常登录使用
- 过期：超过有效期自动过期
- 禁用：永久停用

**数据权限控制**

- 通过SQL语句配置每个账号的数据访问范围
- 支持按地区、时间、货类等维度限制数据访问
- SQL语法基础校验（检查SELECT、语法、引号闭合等）

### 2. 消耗次数统计

统计系统服务使用情况，便于成本核算和额度管理：

**统计维度**

- 按账号统计：查看每个账号的月度使用次数
- 按月份统计：查看每月的总使用次数

**数据展示**

- 列表展示：账号ID、关联CID、客户名称、手机号、月度、月度总提问次数、最后提问时间
- 支持按月份筛选（单选，默认显示最新月份）
- 支持按客户名称、手机号模糊搜索

**数据导出**

- 支持导出Excel格式报表
- 按搜索结果导出：有搜索条件时仅导出筛选后的数据
- 无搜索条件时导出全部数据

**次数重置**

- 每个账号独立显示重置按钮
- 点击后二次确认，仅重置当前账号的提问次数
- 重置后次数清零，最后提问时间显示为"-"

***

## 二、登录功能

新增完整的用户认证体系，保障系统安全访问。

### 1. 登录方式

支持账号密码登录：

- 输入手机号和密码
- 记住密码功能（本地加密存储）
- 登录失败提示：账号不存在、密码错误、账号过期等

### 2. 密码安全策略

**密码复杂度要求**

- 长度8-20位
- 必须同时包含英文字母和阿拉伯数字

**密码修改**

- 用户可在个人中心修改密码
- 需要输入原密码进行验证
- 新密码需符合复杂度要求

### 3. 忘记密码

**流程说明**

- 用户在登录页面点击"忘记密码"
- 进入忘记密码页面，输入注册邮箱和注册手机号
- 系统进行校验：
  - 邮箱格式验证
  - 手机号格式验证
  - 邮箱+手机号是否匹配系统中存在的账号
- 校验通过后显示成功提示："密码已重置并发送至您的邮箱，请查收"
- 校验不成功提示：”校验失败，请检查邮箱和手机号“
- 3秒后自动返回登录页面

**Demo版实现**

- 仅做前端校验，不实际发送邮件
- 使用模拟账号数据进行匹配验证

### 4. 登录状态管理

**Token机制**

- 登录成功后生成token
- token存储在localStorage
- 登录状态8小时未操作自动退出

**退出登录**

- 用户主动点击"退出账号"按钮
- 清除本地存储的token和用户信息
- 刷新页面返回登录界面

### 5. 个人中心

- 右上角用户图标下拉菜单
- 包含"修改密码"和"退出账号"两个选项
- 点击"修改密码"进入修改密码页面
- 点击"退出账号"清除登录状态

***

## 三、分析模板优化

优化"选择分析模板"弹窗的交互体验和内容展示。

### 1. 模板精简

将原有4个分析模板精简为2个核心模板，聚焦最常用场景：

**保留模板**

| 模板名称   | 图标   | 功能描述                  |
| ------ | ---- | --------------------- |
| 货物流向分析 | 定位图标 | 分析区域间货物流动特征、热门线路、流向分布 |
| 上下游分析  | 用户图标 | 分析企业供应链关系、合作伙伴、网络密度   |

**移除模板**

- 选址分析：下线
- 趋势对比分析：下线

### 2. 悬停浮窗交互

为模板卡片增加详细的悬停说明，帮助用户快速了解模板用途。

**交互效果**

- 鼠标进入模板卡片区域，浮窗立即显示
- 浮窗位置跟随鼠标移动，紧贴鼠标指针右下方
- 鼠标离开卡片区域，浮窗立即消失
- 浮窗宽度320px，白色背景，带阴影和圆角边框

**货物流向分析浮窗内容**

```
分析目的：
提供标准化的货物流向分析框架，基于虚拟运单数据从8个维度全面理解货物运输特征，支持区域规划、货类洞察、运力优化等决策需求。

8个分析维度：
• 区域流向维度 - 分析货物从出发地到目的地的流动特征
• 货类流向维度 - 分析不同货类的特定流向特征
• POI类型维度 - 按场站POI类型分析流向
• 时间趋势维度 - 分析月度、季度、年度货物流向变化，识别季节性波动和长期趋势
• 流向集中度维度 - 统计热门线路占比，评估货物流向集中或分散程度
• 距离分布维度 - 按运输距离区间分析特征
• 线路维度 - 分析线路分布及特征
• 车辆属性维度 - 分析参与运输车辆的静态基本信息
```

**上下游分析浮窗内容**

```
分析目的：
提供8个标准化分析维度，支撑企业上下游分析、供应链溯源、行业洞察等关键需求，帮助客户快速理解供应链网络结构、货物流向、关键节点、效率成本及趋势风险。

8个分析维度：
• 企业流量维度 - 量化企业物流活动规模，识别核心物流节点
• 供应链溯源维度 - 追溯货物来源和去向，构建供应链网络
• 行业特征维度 - 分析不同行业的供应链特征和差异
• 龙头识别维度 - 识别行业内的关键企业和市场领导者
• 距离集中度维度 - 量化供应链的空间效率和市场结构
• 装卸效率维度 - 评估供应链节点的操作效率
• 成本效益维度 - 量化供应链的运输成本和效益
• 趋势预测维度 - 分析供应链的时间动态和未来趋势
```

**设计亮点**

- 字体大小分层：标题14px，正文12px，清晰易读
- 列表使用圆点符号，视觉层次分明
- 关键维度名称加粗，便于快速扫描
- 浮窗背景半透明遮罩，不遮挡主界面内容

***

## 四、其他功能调整

### 1. 行业动态监控隐藏

- 从导航菜单中移除"行业动态监控"模块
- 隐藏相关入口和页面

***

## 五、系统管理功能优化（2026-04-02）

### 1. 账号管理页面优化

**操作列位置调整**

- 操作列移至表格首列，便于快速操作
- 列顺序：操作 → 账号ID → 关联CID → 客户名称 → 手机号 → 邮箱 → 状态 → 有效期开始 → 有效期结束 → 创建人

**手机号脱敏显示**

- 默认显示脱敏格式：`138****8001`
- 点击手机号可切换显示/隐藏完整号码
- 使用眼睛图标（Eye/EyeOff）指示当前状态
- 每个账号的解密状态独立管理

**删除功能移除**

- 移除账号删除按钮
- 系统不允许删除账号（仅支持禁用）
- 操作列仅保留"编辑"和"重置密码"功能

### 2. 消耗次数统计页面优化

**导出功能增强**

- 支持按搜索结果导出
- 有搜索条件时：仅导出筛选后的数据
- 无搜索条件时：导出当前月份全部数据

**重置次数优化**

- 移除页面顶部全局重置按钮
- 表格添加"操作"列作为首列
- 每行显示独立的"重置"按钮
- 点击后确认：`确定要重置账号「XXX」的提问次数吗？`
- 仅重置当前行的账号次数，不影响其他账号

